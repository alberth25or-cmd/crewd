{
  "name": "crewd-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Contratos de tesorería por proyecto de Crewd — depósito, liberación por hitos y reembolso pro-rata.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.ts",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network arbitrumSepolia",
    "verify:sepolia": "hardhat verify --network arbitrumSepolia",
    "seed:sepolia": "hardhat run scripts/seed.ts --network arbitrumSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "@types/node": "^22.10.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.17",
    "typescript": "^5.7.2"
  }
}
