# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencias
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# producción
/build

# varios
.DS_Store
*.pem

# depuración
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# entorno — los .example SÍ se versionan, son la documentación de qué hace falta
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# --- contratos (Hardhat) ---
contracts/artifacts/
contracts/cache/
contracts/typechain-types/
# El registro de despliegue de la red efímera de Hardhat cambia en cada
# ejecución y no describe nada real. Los de redes públicas SÍ se versionan:
# son la única fuente de verdad de qué hay corriendo en cada red.
contracts/deployments/hardhat.json

# --- prototipo de referencia ---
# Diseño previo del que se tomó la estructura móvil. No forma parte del build.
yapeinterface/node_modules/

.vercel
